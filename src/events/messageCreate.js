module.exports = async (app, message) => {
    // if(message.author.bot) return;
    // let collector = message.channel.createMessageCollector({max: 1});

    // collector.on("collect", async (mess) => {
    //     if(!mess.content.toLowerCase().includes("gay")){
    //         let words = mess.content.split(" ");
    //         if(words.length == 0){
    //             return;
    //         }else{
    //             let index = words.indexOf(words[words.length - 1]);
    //             words.splice(index, 1);
    //             await message.channel.send(`${words.join(" ")} gay`);
    //         }
    //     }
    // });
}